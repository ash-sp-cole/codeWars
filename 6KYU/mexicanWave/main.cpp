#include<vector>
#include<string>
#include<algorithm>
#include<iostream>
#include<sstream>
#include <cctype>
using namespace std;

int main () {

string str = "hapoppyboi";
string word{};


;

vector <string> answer{};
char k = {};

 // k = toupper(k);



for (int  i = 0 ; i < str.size(); i++) {

str[i] = toupper(str[i]);


}

answer.push_back(word);
cout << answer.at(0);

    return 0;
}